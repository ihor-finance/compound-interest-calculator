import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antigravity.compoundinterest',
  appName: ' Compound Interest',
  webDir: 'dist'
};

export default config;
